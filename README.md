Sync_Saga simulates a real-world data engineering pipeline that:

1.Generates large-scale synthetic data
2.Splits data into batches
3.Processes CPU-heavy tasks using multiple cores
4.Executes I/O-bound tasks using threads
5.Writes results to a JSON file
6.Benchmarks execution time
This project demonstrates how parallelism improves performance in Python.
